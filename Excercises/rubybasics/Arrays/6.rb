# Doubled

numbers = [1, 2, 3, 4, 5]

# Use array.map to iterate and return a new array
# "doubled_numbers" with each number doubled and p
 
doubled_numbers = numbers.map {|x| x * 2}
p doubled_numbers